import { exerciseOneDotTwo } from "./exercise-1.2";

describe('1.2 Translating Math Problems into JS', () => {

    test('returns the correct answer as the formatting is correct', () =>
        expect(exerciseOneDotTwo()).toEqual(-0.24666666666666667));
});
